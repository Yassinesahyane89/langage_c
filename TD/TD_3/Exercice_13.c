/*
Ecrire un programme C qui demande successivement 20 nombres à l’utilisateur, et qui lui
dise ensuite quel était le plus grand parmi ces 20 nombres. Le programme doit afficher aussi le nombre des
maximums ainsi que leurs positions.
*/


#include <stdio.h>
#include <stdlib.h>
void main() {
    int i=0,max,*pos,Nmax;
    printf("donner la valeur %d",i);
    scanf("%d",&n);
    max =n;
    pos=(int*)malloc(sizeof(int));
    *pos=0;
    for(i=1;i<10;)
}
